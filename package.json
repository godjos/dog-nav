{
  "name": "dognav",
  "version": "3.2.0",
  "description": "DogNav - 精选网址导航",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "node --test test/",
    "test:contract": "node --test test/contract.test.js",
    "deploy:cf": "node deploy.js",
    "deploy": "node deploy.js",
    "db:seed": "npx wrangler d1 execute dognav --remote --file=./cloudflare/schema.sql && npx wrangler d1 execute dognav --remote --file=./cloudflare/seed.sql"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sql.js": "^1.10.3",
    "cors": "^2.8.5",
    "multer": "^2.0.2",
    "hono": "^4.4.0"
  }
}
